<% if @searcher.respond_to?(:suggest)  %>
	<% if suggestion = @searcher.suggest %>
	  <p>
	    <%= t(:did_you_mean, :default => "Did you mean") %>
	    <%= link_to h(suggestion), params.merge(:keywords => suggestion) %>?
	  </p>
	<% end %>
<% end %>
